import React, { useState } from "react"; import { Lightbulb, Plus, Zap, Search } from "lucide-react"; const AIIdeation = () => { const [ideas, setIdeas] = useState([]); const [prompt, setPrompt] = useState(""); const [generating, setGenerating] = useState(false); const handleGenerate = async () => { if (!prompt.trim()) return; setGenerating(true); await new Promise(resolve => setTimeout(resolve, 2000)); const newIdea = { id: Date.now(), title: `AI Generated Idea - ${Date.now()}`, prompt, response: "AI generated response here...", createdAt: new Date() }; setIdeas([newIdea, ...ideas]); setPrompt(""); setGenerating(false); }; return ( <div className="p-6 space-y-6"> <div className="flex items-center justify-between"> <div> <h1 className="text-2xl font-bold text-gray-900">AI Ideation Hub</h1> <p className="text-gray-600">Generate AI-powered project ideas</p> </div> <button className="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 flex items-center gap-2"> <Plus className="w-4 h-4" /> New Idea </button> </div> <div className="bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl p-6 border border-blue-200"> <div className="flex items-center gap-3 mb-4"> <Zap className="w-6 h-6 text-blue-600" /> <h2 className="text-xl font-semibold text-gray-900">AI Idea Generator</h2> </div> <div className="space-y-4"> <textarea placeholder="Describe your project idea or challenge..." value={prompt} onChange={(e) => setPrompt(e.target.value)} className="w-full h-32 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none" /> <button onClick={handleGenerate} disabled={!prompt.trim() || generating} className="bg-gradient-to-r from-blue-600 to-purple-600 text-white py-2 px-6 rounded-lg hover:from-blue-700 hover:to-purple-700 disabled:opacity-50 disabled:cursor-not-allowed"> {generating ? "Generating..." : "Generate with AI"} </button> </div> </div> <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"> {ideas.map((idea) => ( <div key={idea.id} className="bg-white rounded-xl border border-gray-200 p-6"> <h3 className="font-semibold text-gray-900 mb-2">{idea.title}</h3> <p className="text-sm text-gray-600 mb-4">{idea.prompt}</p> <div className="text-xs text-gray-500">{idea.createdAt.toLocaleDateString()}</div> </div> ))} </div> </div> ); }; export default AIIdeation;
